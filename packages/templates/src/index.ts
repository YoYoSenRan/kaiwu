export { getAgentDir, listAgentFiles, listTemplates, loadManifest, readAgentFile, validateManifest, validateTemplateIntegrity } from "./loader"
export { ManifestSchema, STAGE_TYPES } from "./types"
export type { AgentDefinition, InitOptions, InitResult, Manifest, PipelineDefinition, StageType, TemplateSummary } from "./types"
